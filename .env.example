# Set to "true" to enable the optional LLM narrative synthesis
USE_LLM=false

# LLM configuration (only needed if USE_LLM=true)
LLM_MODEL=gpt-4o
LLM_API_KEY=
LLM_API_BASE=

# Path to the hidden answer key for scoring (used by evaluate.py)
ANSWER_KEY_PATH=answer_key.json
