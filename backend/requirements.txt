# cognee pins the heavy transitive tree (fastapi, pydantic, litellm, lancedb,
# kuzu, sqlalchemy ...). Keep our own deps as lower-bounds so pip can resolve
# them against whatever cognee requires.
cognee==1.1.2
fastembed>=0.5.0
fastapi>=0.116.2
uvicorn[standard]>=0.34.0
pandas>=2.2.0
python-dotenv>=1.0.1
openai>=1.59.0
networkx>=3.4
